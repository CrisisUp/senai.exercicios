[package]
name = "gestor_frota_pro"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.0", features = ["full"] }
tokio-rusqlite = "0.6"
async-trait = "0.1"
# Removida a dependência direta de rusqlite para evitar conflito de libsqlite3-sys
