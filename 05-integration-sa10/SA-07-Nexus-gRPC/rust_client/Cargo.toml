[package]
name = "nexus-grpc-client"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic = "0.11"
prost = "0.12"
tokio = { version = "1.36", features = ["full"] }

[build-dependencies]
tonic-build = "0.11"
